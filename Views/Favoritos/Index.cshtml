@model IEnumerable<SosDog.Models.Favorito>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Usuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ocorrencia)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Usuario.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ocorrencia.Descricao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Favorito">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Favorito">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Favorito">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
